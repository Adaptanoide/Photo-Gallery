// src/routes/marketing.js
const express = require('express');
const router = express.Router();

console.log('📧 Marketing routes loaded (minimal - only for Constant Contact sync)');

// Todas as rotas foram removidas
// Email marketing agora é feito diretamente no Constant Contact
// A sincronização de contatos roda automaticamente via AutoSyncService

module.exports = router;